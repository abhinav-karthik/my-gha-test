name: this is side title
run-name: this is run title
on: 
  pull_request:
    types: labeled
  issues:
    types: labeled
jobs:
  build:
   runs-on: ubuntu-latest
   container:
      image: python
   steps:
     - name: greet
       run: hello world
   
